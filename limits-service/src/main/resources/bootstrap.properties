#if we don't use below property then it will pick up the default name as "application" instead of limits-service
#This name is used by server config for specifiying particular service name property file
spring.application.name=limits-service
spring.cloud.config.uri=http://localhost:8888
#Below property is used for specific environment else dafault will be picked up
spring.profiles.active=dev

management.endpoints.web.exposure.include=*

#eureka.client.service-url.default-zone=http://localhost:8761/eureka